#!/usr/bin/env node

/**
 * Plugwise MCP Server CLI Entry Point
 *
 * This is the executable entry point for the Plugwise MCP Server when installed via npm.
 * It starts the server which provides MCP tools for Plugwise smart home integration.
 */

import "../build/mcp/server.js";
